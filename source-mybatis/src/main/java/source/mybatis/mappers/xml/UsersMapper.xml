<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="source.mybatis.mappers.xml.UsersMapper">
    <cache/>
    <select id="selectById" parameterType="Integer" resultType="source.mybatis.domain.Users">
    select * from users where id = #{id}
  </select>
</mapper>

